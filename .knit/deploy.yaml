version: 1
on:
  push:
    deployments:
      - target: dev
        cwd: /usr/src/eliware/test
        commands:
          - npm ci
          - npm test
          - npm run lint
          - npm audit --omit=dev --audit-level=moderate
          - npm pack --dry-run
